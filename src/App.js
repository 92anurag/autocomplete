import React from 'react';
import AutoComplete from './AutoComplete/AutoComplete';

// Best place to initialize routers and redux, if using in the project
// This is the main
const App = () => {
	return <AutoComplete />;
};

export default App;
